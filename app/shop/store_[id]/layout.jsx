export const metadata = {
  title: 'Shop — innovates.bt',
}

export default function ShopLayout({ children }) {
  return (
    <div className="min-h-screen bg-[#0F172A] text-gray-100">
      {children}
    </div>
  )
}
